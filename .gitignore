node_modules/
.env
dist/
out/
*.log

# never commit key backups
.env.*
!.env.example

# Uploaded photos and videos — user data, never commit.
# Anchored with a leading slash: a bare `media/` also matches src/media/,
# which silently excluded real source files.
/media/

# vite build output
web/dist/

.ddb-local/
